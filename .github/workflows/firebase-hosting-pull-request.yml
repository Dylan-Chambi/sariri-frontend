# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - name: install UI
        run: npm install @mui/material @emotion/react @emotion/styled --legacy-peer-deps
      - name: INstall ui 2
        run: npm install @mui/icons-material --legacy-peer-deps
      - uses: actions/checkout@v2
      - name: install
        run: npm install @mui/material @emotion/react @emotion/styled && npm install
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SARIRI_7DF51 }}'
          projectId: sariri-7df51
